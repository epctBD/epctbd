import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    colorPrimary: "#3395F7",
    fontSize: 16,
    borderRadius: 8,
    screenMD: 769,
  },
};

export default theme;
